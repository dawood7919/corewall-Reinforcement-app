# منصة الإنشاء الإبداعي داخل Core Wall

## الملخص التنفيذي

الفكرة المقترحة هي تحويل Core Wall من تطبيق لإدارة ملفات ومشاريع الإنشاءات إلى **منصة إنتاج مستندات ومواد مرئية يقودها الذكاء الصناعي**. يكتب المستخدم طلبه بلغة طبيعية، مثل: «أنشئ تقرير فحص للعمود C-14، أضف صور الموقع، أدرج نتائج الحصر، ثم صدّره PDF»، فيحوّل الذكاء الطلب إلى خطة واضحة، ويستدعي أدوات التطبيق لإنشاء المحتوى وتنسيقه، ثم يعرض معاينة قبل الحفظ أو المشاركة.

الأفضل ألا يكون الذكاء الصناعي هو الذي يرسم الملف أو يكتب مباشرة داخل صيغة Word/PDF. النموذج يقترح المحتوى والهيكل والقرارات التصميمية، بينما ينفذ **محرك مستندات حتمي** عملية التخطيط والتنسيق والتصدير. بهذه الطريقة نحصل على نتيجة قابلة للتكرار والمراجعة، ولا يتغير شكل التقرير عشوائياً بين تشغيل وآخر.

> المعادلة الصحيحة: **الذكاء يخطط ويكتب ويختار؛ محرك المستندات ينفذ؛ المستخدم يعاين ويعتمد؛ التطبيق يحفظ المصدر والنسخة النهائية.**

## ما الذي ينبغي أن يصنعه التطبيق؟

| نوع المخرج | الاستخدام في Core Wall | الناتج المقترح |
|---|---|---|
| مستند قابل للتحرير | تقارير الفحص، محاضر الاجتماعات، خطابات المقاول، تقارير يومية | ملف مصدر منظم قابل لإعادة الفتح والتحرير، ثم تصدير DOCX أو صيغة تحرير داخلية |
| PDF احترافي | تقارير الجودة، تقارير الحصر، سجل الملاحظات، محاضر التسليم | PDF متعدد الصفحات مع غلاف، فهرس، جداول، صور، توقيعات، ومراجع المصادر |
| صور وتقارير مرئية | لوحات تقدم، قبل/بعد، ملصقات تنبيه، بطاقات بند، أغلفة التقارير | PNG أو JPEG بجودة مناسبة للمشاركة والطباعة |
| حزمة مشروع | تقرير PDF مع الصور والملفات والملخص التنفيذي | مجلد أو ZIP منظم باسم الدور والمشروع وتاريخ الإنشاء |

يجب أن يكون لكل ملف **مصدر قابل لإعادة التوليد**، وليس مجرد ملف نهائي. إذا تغيرت نتيجة فحص أو أضيفت صورة جديدة، يستطيع المستخدم تحديث التقرير وإعادة تصديره من دون بنائه يدوياً من البداية.

## السيناريو الرئيسي للمستخدم

يبدأ المستخدم من مركز قيادة الذكاء ويكتب طلباً واحداً. يفهم الوكيل الدور الحالي والمشروع والملفات والملاحظات والقياسات المتاحة، ثم يعرض خطة من خطوات، مثل قراءة مستندات الدور، اختيار الصور المرتبطة، استخراج البنود الحرجة، إنشاء هيكل التقرير، تطبيق قالب الجودة، ثم إنشاء معاينة PDF.

بعد اعتماد الخطة، ينشئ التطبيق **مسودة تفاعلية**. يستطيع المستخدم تغيير العنوان، حذف صورة، تغيير ترتيب الأقسام، اختيار قالب، تعديل اللغة، أو طلب إعادة صياغة جزء واحد فقط. بعد الموافقة على المعاينة، يحفظ التطبيق المصدر وينشئ PDF أو صورة أو حزمة مشاركة.

## المعمارية المقترحة

### طبقة 1: نية المستخدم وخطة التنفيذ

تستقبل هذه الطبقة طلباً مثل «اعمل تقرير صبّة اليوم». يحوله الذكاء إلى `CreationPlan` يحتوي على نوع المخرج، مستوى الدور، مصادر البيانات، خطوات الإنشاء، قالب العرض، والعمليات التي تحتاج موافقة. لا يسمح النموذج بإنشاء مسار ملف أو تنفيذ عملية مشاركة من تلقاء نفسه؛ كل ذلك يمر عبر أدوات typed ومتحقق منها.

### طبقة 2: سياق المشروع والذاكرة

تجمع الطبقة بيانات الدور، عناصر التسليح، القياسات، الملاحظات، ملفات PDF المحللة، الصور، المهام، حالات الفحص، وأي تقرير سابق. يجب تسجيل مصدر كل حقيقة داخل المستند؛ فالرقم المستخرج من حصر أو PDF يظهر في البيانات الداخلية مع مرجع العنصر أو الملف والوقت الذي قُرئ فيه.

### طبقة 3: نموذج المستند الداخلي

بدلاً من بناء Word أو PDF مباشرة من نص المحادثة، ينشئ الذكاء نموذجاً محايداً للصيغة، مثل:

```text
Document
  metadata
  theme
  sections[]
    heading
    paragraphs[]
    tables[]
    images[]
    callouts[]
    signatures[]
  sourceReferences[]
```

هذا النموذج يسمح بإخراج PDF وصورة وتنسيق قابل للتحرير من نفس المصدر. كما يمنع اختلاف المحتوى بين صورة التقرير ونسخة PDF، لأن كلاهما يصدر من نفس النموذج.

### طبقة 4: محرك التنسيق والرندر

ينفذ المحرك قواعد الصفحات، الهوامش، الخطوط العربية، اتجاه RTL، التفاف النص، جداول الصفحات، وضع الصور، ترقيم الصفحات، العناوين، الألوان، والتوقيعات. يجب أن يعمل بدون اعتماد على قرارات النموذج أثناء الرندر؛ لأن الرندر يحتاج نتيجة ثابتة وسهلة الاختبار.

### طبقة 5: التخزين والإصدار

يخزن التطبيق مسودة المستند، نسخة من قالب التصميم، سجل المصادر، النسخة النهائية، ووقت التصدير. عند إعادة فتح الملف يرى المستخدم آخر مسودة ونسخ التصدير السابقة، ويستطيع إنشاء نسخة جديدة بدلاً من الكتابة فوق النسخة القديمة.

## الأدوات التي يجب أن يضيفها عقل التطبيق

| الأداة | الوظيفة | الموافقة المطلوبة |
|---|---|---|
| `create_document` | إنشاء مسودة من نوع تقرير أو خطاب أو محضر | موافقة قبل الحفظ النهائي |
| `update_document_section` | إعادة كتابة قسم أو تغيير لغته أو نبرته | معاينة التغيير |
| `insert_project_data` | إدراج قياسات، بنود، نتائج فحص، أو جدول حضور مع مصدره | معاينة تلقائية وموافقة على التعديل |
| `insert_media` | إضافة صور أو صفحات PDF أو رسم S Pen | موافقة إذا كانت الصور خارج الدور الحالي |
| `apply_template` | تطبيق قالب تقرير أو خطاب أو غلاف | يمكن تنفيذه داخل المسودة |
| `create_table` | إنشاء جدول من بيانات الدور أو نتائج الحصر | معاينة الجدول |
| `create_chart` | إنشاء رسم بياني من بيانات حقيقية داخل التطبيق | معاينة القيم والمصدر |
| `generate_image` | إنشاء صورة غلاف أو لوحة توضيحية أو ملصق | معاينة قبل الحفظ |
| `export_pdf` | إخراج PDF نهائي | موافقة التصدير |
| `export_editable` | إخراج نسخة قابلة للتحرير | موافقة التصدير |
| `share_file` | فتح شاشة المشاركة أو إنشاء حزمة | تأكيد صريح دائماً |
| `duplicate_version` | إنشاء نسخة جديدة من مستند سابق | يمكن تنفيذه بعد اختيار المستخدم |

## إنشاء الصور بواسطة الذكاء الصناعي

ينبغي فصل الصور الواقعية عن الصور التوضيحية. صور الموقع، صور العيوب، وصفحات PDF يجب أن تبقى كما هي دون تعديل توليدي قد يغير الدليل الهندسي. أما صور الغلاف، الأيقونات، المخططات التوضيحية، الخلفيات، ورسوم شرح طريقة العمل فيمكن إنشاؤها أو تحسينها بواسطة الذكاء الصناعي، مع وسمها بوضوح بأنها **صورة مولدة أو توضيحية**.

يقترح النظام على المستخدم نوع الصورة قبل التنفيذ: صورة دليلية أصلية، صورة معالجة محافظة على المحتوى، أو صورة توضيحية مولدة. ولا يجب أن يستخدم الذكاء الصناعي صورة مولدة باعتبارها دليلاً على حالة موقع أو نتيجة فحص.

## القوالب المقترحة للمرحلة الأولى

| القالب | الأقسام الافتراضية |
|---|---|
| تقرير فحص جودة | الغلاف، بيانات المشروع، نطاق الفحص، الصور، الملاحظات، الإجراءات التصحيحية، الاعتماد |
| تقرير حصر كميات | ملخص تنفيذي، الرسمات، المعايرة، الجداول، الكميات حسب الدور، الملاحظات، مصادر البيانات |
| تقرير يومي | التاريخ، العمالة، الأعمال المنفذة، المواد، الصور قبل/بعد، المعوقات، المهام التالية |
| محضر اجتماع | الحضور، القرارات، البنود المفتوحة، المسؤول، الموعد، المرفقات |
| خطاب رسمي | الجهة، الموضوع، المرجع، متن الخطاب، المرفقات، التوقيع |
| بطاقة عنصر | اسم العنصر، الدور، حالة الفحص، القياسات، الصور، الملاحظات، QR أو مرجع الملف |

## تجربة الاستخدام المقترحة

يحتاج التطبيق إلى شاشة باسم **استوديو الإنشاء** داخل مركز قيادة الذكاء. تحتوي الشاشة على زر «أنشئ شيئاً»، وقوالب جاهزة، ومستندات حديثة، وملفات مولدة، ومسودات تنتظر الاعتماد. عند كتابة الطلب، يظهر للمستخدم شريط يوضح المصادر التي سيستخدمها الذكاء، مثل «3 ملفات PDF محللة، 8 صور، 12 بند حصر».

أثناء التخطيط، يرى المستخدم خطوات الخطة وبجانب كل خطوة نوعها: قراءة، تحليل، كتابة، تصدير، أو مشاركة. أثناء المسودة، تتحول الواجهة إلى محرر صفحة كاملة مع شريط تنسيق ثابت يدعم العناوين، النص الغامق، الألوان، الجداول، الصور، الرسم بالقلم، والفواصل. زر «تحسين بالذكاء» يعمل على الجزء المحدد فقط، ولا يعيد كتابة المستند كله من دون طلب واضح.

## الضوابط المهنية

يجب منع الاختلاق في الأرقام. أي رقم أو حالة أو اسم عنصر يدرج في تقرير المشروع يجب أن يرتبط بمصدر داخل التطبيق. إذا لم يجد الذكاء قيمة، يكتب «غير متوفر» أو يطلب من المستخدم إدخالها، ولا يملأها برقم تخميني.

كما يجب الفصل بين المستندات الدليلية والمخرجات الإبداعية. لا يجوز تعديل صورة عيب أو نتيجة فحص بطريقة تخفي تفاصيلها. ويجب أن يظهر سجل صغير داخل خصائص المستند يوضح من أنشأه، ومتى، وما هي الملفات والبيانات المستخدمة، وما إذا كانت أي صورة مولدة.

## خطة التنفيذ العملية

### المرحلة الأولى: الأساس القابل للتسليم

ننشئ نموذج المستند الداخلي، شاشة استوديو الإنشاء، ثلاثة قوالب أولية، ومعاينة PDF. في هذه المرحلة يكون إنشاء المحتوى من بيانات الدور والملاحظات والملفات المحللة، مع حفظ المسودة محلياً وسجل المصادر.

### المرحلة الثانية: محرك المستندات متعدد الصيغ

نضيف إخراج PDF مضبوطاً للعربية، وإخراج صورة للصفحة أو الغلاف، وتصديراً قابلاً للتحرير، مع دعم الجداول والصور والتوقيعات وترقيم الصفحات. الأولوية للجودة والثبات قبل إضافة عدد كبير من القوالب.

### المرحلة الثالثة: ربط عقل التطبيق

نضيف أدوات الإنشاء إلى كتالوج الوكيل. يصبح الطلب الطبيعي خطة `CreationPlan`، وتظهر خطواتها في مركز القيادة، ويستطيع الذكاء قراءة بيانات الدور وإدراجها مع المراجع. كل إخراج نهائي يحتاج اعتماداً، بينما يمكن إعادة صياغة مسودة داخلية دون مشاركة أو حذف.

### المرحلة الرابعة: الصور والرسوم المتقدمة

نضيف مولد أغلفة وصور توضيحية ومخططات من بيانات حقيقية. كما نضيف تحسين الصور الأصلية بشكل محافظ، مع حفظ الصورة الأصلية وعدم استبدالها. بعد ذلك يمكن بناء حزم تقارير ZIP ومشاركة ملف واحد أو مجموعة ملفات.

## معايير الجودة

| الاختبار | معيار القبول |
|---|---|
| ثبات الأرقام | كل قيمة في التقرير مرتبطة بمصدر أو موسومة بأنها غير متوفرة |
| العربية وRTL | لا تتداخل الحروف أو الجداول، ويظل اتجاه القراءة صحيحاً |
| الصور | لا تختفي الصور أو تنقلب بعد التصدير، وتبقى الصورة الأصلية محفوظة |
| الصفحات | لا ينقطع عنوان عن جدوله أو توقيع عن النص المرتبط به دون قاعدة واضحة |
| إعادة التوليد | إعادة التصدير من نفس المسودة تعطي محتوى متطابقاً |
| الأمان | لا مشاركة أو حذف أو تعديل حساس دون موافقة صريحة |
| الأداء | إنشاء المعاينة لا يعيد تحليل كل ملفات الدور في كل مرة |
| التتبع | يظهر مصدر كل معلومة ونسخة القالب ووقت الإنشاء |

## القرار المقترح

أوصي بالبدء بمنتج واحد واضح: **منشئ تقارير Core Wall**. لا نبدأ بمحرر Word عام ينافس البرامج المكتبية، بل نبني محرراً متخصصاً في تقارير الإنشاءات يستخدم بيانات التطبيق فعلياً. بعد ثبات تقارير الفحص والحصر والتقرير اليومي، نضيف الخطابات والمستندات العامة، ثم نوسع الصور التوليدية.

هذا المسار يحقق قيمة عملية أسرع، ويجعل الذكاء الصناعي عصباً حقيقياً للتطبيق بدلاً من مجرد مولد نصوص. كما يضمن أن الملفات الناتجة تحمل بيانات المشروع ومصادرها وصورها وتنسيقها، وليس محتوى عاماً يمكن إنشاؤه في أي محرر آخر.
