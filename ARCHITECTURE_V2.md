# قرار معمارية Core Wall V2

## القرار التنفيذي

تُبنى واجهات التطبيق في **Jetpack Compose**، لكن مساحة العمل عالية التردد لا تُبنى كـCompose Canvas. تستضيف Compose سطح `PdfWorkspaceView` أصلياً عبر `AndroidView`، وهو المالك الوحيد لحالة التكبير والسحب والطبقات والرسم. هذا يمنع تحديثات المنظر المتكررة من إعادة تركيب شجرة الأزرار والقوائم وواجهة التطبيق.

يبقى **PDFium** طبقة الرندر التفاعلي في V2 لأن التطبيق يعتمد على طبقة النص والبحث والتحديد على أجهزة تبدأ من Android 8 (API 26). تظل **PDFBox** لعمليات الخلفية المنظمة مثل الدمج والتصدير والعلامة المائية، ولا يسمح لها بالدخول إلى مسار الإيماءات أو الرسم الحي.

## سبب عدم اعتماد AndroidX PDF Viewer كبديل مباشر

تقدم مكتبة AndroidX PDF Viewer عارضاً مضمناً بميزات البحث والاختيار والتكبير والدعم بالقلم، وهي مبنية على واجهات Android الأساسية.[1] لكن مسار V2 يحتاج تحكمًا دقيقًا في بلاطات الرسم الهندسي والطبقات المتجهة والقياس وS Pen، مع دعم Android 8. لذلك ستكون مرجعاً وظيفياً للمقارنة وليست سطح العرض النهائي.

واجهة `PdfRenderer` الرسمية تسمح برندر جزء من الصفحة إلى Bitmap مع تحويل، لكن المحرك يفتح صفحة واحدة فقط في كل وقت ويُنفذ الوصول بشكل متسلسل.[2] هذه الخصائص تؤكد قرار V2: **مجدول محدود بطابور أولوية**، لا إطلاق مهام موازية اعتباطية.

## بنية الحزم الجديدة

| الحزمة | المسؤولية | ممنوعات صريحة |
|---|---|---|
| `v2.pdf.session` | فتح ملف واحد، المقاسات، دورة الحياة، إغلاق الموارد. | لا حالة واجهة ولا رندر على Main. |
| `v2.pdf.viewport` | التحويلات والإيماءات والاندفاع وتقييد الحدود. | لا Compose State في كل حركة. |
| `v2.pdf.tiles` | مفتاح البلاطة، طابور الأولوية، الإلغاء، LRU بالبايتات. | لا Bitmap ضخمة للصفحة كاملة. |
| `v2.pdf.render` | عرض البلاطات من الذاكرة وتحديث المناطق المتأثرة فقط. | لا فك JSON ولا معالجة أدوات. |
| `v2.pdf.overlay` | التعليقات والقياسات والاختيار والحبر المؤقت. | لا طلب رندر PDF عند تغيير Overlay. |
| `v2.pdf.perf` | عدادات الفتح والرندر والكاش والإطار والذاكرة. | لا لوحة قياس تعمل في كل إطار. |
| `v2.feature.*` | المشاريع والحصر والملاحظات والتقارير. | لا اعتماد مباشر على مكتبات الرندر. |

## قرار الترحيل

ينشأ V2 بجانب المسارات الحالية أولاً. ترحّل فقط نماذج البيانات وحسابات الحصر والقياسات ووثائق الملاحظات، ثم تتحول نقطة الدخول بعد المرور باختبارات الترحيل والأداء. لا تُحذف الجداول أو الواجهات القديمة أثناء تأسيس محرك V2.

## المراجع

[1] [Android PDF viewer — Android Developers](https://developer.android.com/develop/ui/views/layout/pdf/pdf-viewer)

[2] [PdfRenderer API reference — Android Developers](https://developer.android.com/reference/kotlin/android/graphics/pdf/PdfRenderer)
