# Core Wall V2 — خطة إعادة البناء

## القرار

لن تُنقل شاشة PDF أو حلقة الرسم أو حالة الإيماءات القديمة إلى النسخة الجديدة. ستبنى النسخة الثانية على **محرك عرض مستقل عن Compose**: تستخدم Compose للواجهات الثابتة وأشرطة الأدوات، بينما يعمل سطح PDF كـ`View` متخصص يحتفظ بحالة المنظر والبلاطات واللمس خارج دورة إعادة التركيب. لا يبدأ ترحيل أي شاشة إلى V2 قبل أن تكون مرحلة PDF قابلة للقياس على ملف رسم فعلي.

## مبدأ المنتج

التطبيق أداة ميدانية للهندسة، وليس تجميعة شاشات. المسار الأساسي هو: **مشروع → ملف رسم → تكبير/سحب سريع → قياس أو تعليق → حفظ محلي فوري → تصدير**. لهذا تعطى أولوية زمن الإطار، والدقة المكانية، وإدخال S Pen على حساب المؤثرات البصرية غير الضرورية.

## مصفوفة الوظائف

| المجال | وضع V2 | قرار الترحيل |
|---|---|---|
| فتح PDF والبحث والنص | نواة جديدة حول جلسة مستند واحدة ومحرك رندر منفصل | يعاد بناؤه، لا ينقل Canvas القديم. |
| التكبير والسحب والاندفاع | `ViewportController` مستقل وتحويل أولاً | يعاد بناؤه بالكامل خارج حالة Compose السريعة. |
| بلاطات PDF | `TileScheduler` + LRU bounded cache + طلبات مرئية أولاً | يعاد بناؤه مع مقياس دقة متدرج وإلغاء الطلبات القديمة. |
| القياس والحصر | طبقة متجهات منفصلة وإحداثيات موحدة | ترحّل نماذج البيانات وحسابات المجال فقط. |
| الرسم والتعليقات وS Pen | طبقات Overlay مستقلة مع dirty regions | يعاد بناء الإدخال والرسم؛ ترحّل البيانات المحفوظة. |
| المشاريع والملفات | واجهات Compose جديدة مع مستودعات البيانات الحالية مؤقتاً | تعاد الواجهة، ويُستبقى الوصول للبيانات. |
| الملاحظات | مستند كتل محلي وتدفق مرفقات | يرحّل نموذج الوثيقة والملفات؛ تعاد الواجهة عند الحاجة. |
| أدوات PDF | عمليات خلفية مستقلة عن العارض | تظل PDFBox للدمج/التصدير/العلامة المائية، ولا تدخل مسار العرض. |
| إعدادات وقاعدة البيانات | Room بترحيلات متوافقة | تحافظ على البيانات؛ لا تحذف أي جدول قبل تحقق الترحيل. |

## معمارية V2

```text
Compose shell / screens
        │
        ├── Project & document repositories
        │
        └── PdfWorkspaceHost (Android View)
                ├── ViewportController
                ├── PdfDocumentSession
                ├── TileScheduler
                ├── TileMemoryCache
                ├── PdfBaseLayer
                ├── AnnotationLayer
                ├── MeasurementLayer
                └── InteractionLayer
```

## قواعد أداء لا يمكن تجاوزها

1. لا يُرستر PDF ولا يُفك JSON ولا تُنشأ Bitmap داخل خيط الواجهة.
2. لا تقرأ Compose حالة `scale` أو `translation` في كل حركة إصبع؛ محرك العرض نفسه يطبق التحويل.
3. إيماءة التكبير تحول البلاطات الموجودة أولاً؛ الجودة العالية تأتي لاحقاً على شكل ترقيات بلاطات مرئية فقط.
4. التعليق أو القياس لا يعيد رندر الصفحة؛ يُبطل مستطيل الطبقة المتأثرة فقط.
5. الكاش محدد بعدد بايتات وليس بعدد عناصر فقط، ويحرر Bitmap غير المرئية بطريقة LRU.
6. كل طلب بلاطة يحمل جيلاً وأولوية، وتُلغى الأعمال المنتظرة عند تغير المنطقة المرئية.
7. المؤثرات البصرية في الواجهات تكون محدودة، ولا تستخدم Blur أو Alpha متحرك فوق مساحة الرسم.

## معايير القبول

| المقياس | الحد الأدنى لقبول V2 |
|---|---|
| أول محتوى مرئي | معاينة صفحة قبل بدء الجودة العالية. |
| التكبير | لا توجد إعادة رندر صفحة كاملة مع كل إطار إيماءة. |
| السحب | يظل التحويل متجاوباً مع وجود رندر خلفي. |
| الذاكرة | كاش بلاطات محدد بالبايتات مع إطلاق الموارد عند الإغلاق. |
| القياس | الإحداثيات لا تنحرف بعد تكبير أو تدوير أو تغيير حجم الشاشة. |
| الحفظ | تُرحّل المشاريع والقياسات والملاحظات الموجودة من دون حذف. |
| التحقق | لا يُدّعى تحسن الأداء من دون سجل قياسات لملف رسم حقيقي. |
