# تدقيق محرك PDF — Core Wall

## نطاق التدقيق

يتكون المسار الحالي من `PdfViewerScreen` لتجميع الواجهة، و`PdfCanvas` للرسم والإيماءات، و`PdfViewerState` للتحويلات والتخطيط، و`TileEngine` للتخزين والرندر، و`PdfDocumentSession` لتغليف PDFium بخيط أصلي واحد. تتصل طبقات التعليقات والقياسات والاختيار بالرسم فوق بلاطات PDF، بينما تبقى عمليات الدمج والتصدير والعلامة المائية وOCR خارج مسار العرض الحي.

## نتائج السبب الجذري

| المجال | الملاحظة الفعلية | أثرها المتوقع على الرسمات الهندسية |
|---|---|---|
| طابور الرندر | كانت الطلبات توضع كلها فوراً على منفذ PDFium أحادي الخيط؛ تصبح الأولوية قديمة بعد تغيير اتجاه الحركة. | انتظار بلاطات الموضع الجديد خلف أعمال لم تعد مرئية. |
| حالة العرض | الإزاحة والتكبير والحجم وحالة التفاعل كلها حالات Compose سريعة التغير. | احتمال إعادة تركيب مكونات واجهة تقرأ القيم مع كل إطار إيماءة. |
| الطبقات العلوية | كانت نقاط التعليقات والقياسات تفك من JSON وتفحص على مستوى المستند داخل مسار الرسم. | ضغط CPU وتخصيصات وGC عند وجود ملاحظات أو قياسات كثيرة. |
| تخطيط الصفحات | كان اختيار الصفحات المرئية يعتمد على مسح كامل قائمة الصفحات. | تكلفة تتزايد مع ملفات مئات الصفحات. |
| تحديث الجودة | لا يوجد صف صريح «أحدث منطقة أولاً»؛ الطبقة الحادة قد تتأخر عند تغير المنظر بسرعة. | إحساس ضعيف بالـrefresh أو تأخر الوضوح بعد التكبير. |
| منافسة الأعمال | المصغرات وبعض واجهات الأدوات يمكن أن تستخدم نفس جلسة PDFium. | منافسة بلاطات الموضع المرئي على الخيط الأصلي الوحيد. |

## مصفوفة الحفاظ على الميزات

| الوظيفة | المسار الحالي | المطلوب بعد إعادة الهندسة |
|---|---|---|
| فتح PDF والتنقل بين الصفحات | `PdfDocumentSession` و`PdfViewerState` | الحفاظ عليه مع أول معاينة سريعة وجلسة واحدة للملف. |
| التكبير والسحب والاندفاع | `PdfGestures` و`PdfCanvas` | تحويل فوري أولاً، ثم رندر بلاطات مرئي فقط بأولوية حديثة. |
| القلم وS Pen | `PdfCanvas` وطبقات الأدوات | فصل الإدخال والرسم المؤقت عن بلاطات المستند. |
| التعليقات | `PdfAnnotationEntity` وطبقة الرسم | تبقى طبقة مستقلة ولا تطلب رندر PDF. |
| القياسات والمعايرة | `PdfMeasurementEntity` و`PdfMeasureLayer` | تبقى طبقة مستقلة ومجمعة حسب الصفحة المرئية. |
| البحث والتحديد | `PdfText` و`PdfSelectionLayer` | لا تعمل أثناء الإيماءة إلا عند طلب المستخدم. |
| المصغرات والخريطة | `PdfNavigation` | تعمل بأولوية خلفية ولا تنافس بلاطات الموضع المرئي. |
| أدوات الملف | `PdfOps` وواجهات التصدير | تبقى خارج جلسة العرض الحي ولا تحجب خيط الواجهة. |

## قرار المعمارية

يبقى **PDFium** محرك الرستر التفاعلي لأنه هو المستخدم فعلياً ومقيد بخيط واحد بأمان. تبقى **PDFBox** لعمليات تعديل/تصدير الملفات فقط، وليس لرسم الصفحات. ستفصل إعادة الهندسة مسؤوليات الطلبات إلى متحكم/مجدول ذي أولوية حديثة، وذاكرة بلاطات محددة، وحالة عرض عالية التردد لا تقرأها واجهات الأدوات مباشرة.

## القياسات المطلوبة قبل الادعاء بالأداء

لن تُذكر أرقام فتح أو FPS أو P95 كأرقام منجزة قبل تشغيلها على ملف رسم حقيقي وجهاز فعلي. ستضاف طبقة تشخيص debug لزمن فتح المستند، أول معاينة، زمن البلاطة، إصابات الكاش، الإلغاءات، عدد الطلبات، والذاكرة. يلزم تشغيلها على ملف A0 أو CAD كثيف لتسجيل مقارنة قبل/بعد ذات معنى.
