# مراجعة قياس ملفات DWG

## الملاحظة الحاسمة في التطبيق الحالي

عارض CAD الحالي يرسم **DXF النصي فقط** (LINE وPOLYLINE وCIRCLE وARC وTEXT/MTEXT). ملفات DWG الثنائية التي تبدأ بتوقيع `AC10` أو `AC1.` لا تُحلل؛ تظهر رسالة تطلب التحويل إلى DXF أو الفتح بتطبيق خارجي. لذلك لا يمكن وصفه حالياً بأنه عارض DWG حقيقي.

## عناصر تجربة DWG FastView المفيدة

صفحة DWG FastView تصف تجربة تركز على فتح DWG/DXF بسرعة، العمل دون تسجيل، الاحتفاظ بالرسومات محلياً، السحب والتكبير باللمس، القياس، الأبعاد، الطبقات، التخطيطات، وضبط دقة عرض المسافة والزاوية. أهم العناصر التي تناسب Core Wall، لأن الطلب مقصور على الأبعاد، هي: فتح الرسم بلا تعقيد، تكبير/تحريك مستقل، التقاط نقاط هندسية، معايرة بطول معروف، عرض نتيجة القياس فوق الرسم، والتراجع السريع.

المصدر: https://play.google.com/store/apps/details?id=com.gstarmc.android

## نتيجة مراجعة محرك DWG

دعم DWG الثنائي ليس امتداداً بسيطاً لمحلل DXF؛ فهو يتطلب قارئاً مرخصاً/مخصصاً أو مسار تحويل موثوقاً. مثال مرجعي: Nutrient يدعم DWG/DXF عبر محرك تحويل على الخادم أو SDK للـ.NET/Java/Python، ثم يقدم القياس في عارض الويب بعد التحويل إلى PDF. لا يقدم هذا المثال مسار Android أصلي مجاني جاهز داخل التطبيق.

المصدر: https://www.nutrient.io/sdk/solutions/cad/

## القرار المعماري المقترح

1. **مسار DXF محلي فوري:** تحسين العارض القائم كـ DXF FastMeasure؛ يصبح قياس المسافة هو الأداة الأساسية، مع التقاط end/mid/intersection، ومعايرة وحفظ القياسات لكل ملف.
2. **مسار DWG حقيقي:** عزل `CadDocumentSource` عن العارض. يسلم طبقة هندسية موحدة سواء أتت من DXF محلي أو DWG من محرك مرخص/خدمة تحويل. لا تُخفى حدود DWG ولا يعرض التطبيق قياسات وهمية عندما لا يتوفر محرك القراءة.
3. **مسار بديل آمن الآن:** عند استيراد DWG ثنائي، يطلب التطبيق من المستخدم تحويله إلى DXF أو فتحه خارجياً، مع حفظ الملف وبياناته. عند إدخال محرك DWG لاحقاً، لا تتغير تجربة القياس ولا طبقة الحفظ.

## نطاق القياس المنفذ لاحقاً

الأولوية هي طول بين نقطتين، معايرة، اقتناص نقاط، تراجع ومسح، وعلامة طول قابلة للحفظ والتصدير. المساحة والزاوية ونصف القطر تبقى قدرات ثانوية ولا تكون محور الواجهة.

## مسارات DWG الفعلية التي تمت مراجعتها

| المسار | طبيعة الدعم | ملاءمته للتطبيق |
| --- | --- | --- |
| **ODA Drawings SDK** | SDK أصلي مرخص يتيح الوصول إلى بيانات DWG/DXF ويدعم Android، بما فيه arm64-v8a. | الخيار الأقوى للعرض المحلي الدقيق وقياس الهندسة دون رفع الملف، لكنه يتطلب ترخيصاً ودمجاً عبر NDK. |
| **DWG FastView Mobile SDK** | مكوّن Android/iOS مرخص للعرض، التكبير، القياس، الطبقات والتخطيطات. | أقرب تجربة جاهزة لطلب المستخدم، لكنه منتج مؤسسي يتطلب اتفاق SDK ومفاتيح/حزمة مزود. |
| **Autodesk Model Derivative** | خدمة خلفية لتحويل التصاميم إلى صيغ عرض واستخراج بياناتها. | مناسب لخيار سحابي لاحقاً، لكنه لا يحقق قراءة DWG محلية مستقلة ولا يصلح كبديل فوري لعارض Android الميداني. |

## القرار التنفيذي المحدث

يكون دعم DWG الحقيقي عبر **موفر هندسة قابل للاستبدال** وراء `CadDocumentSource`: يبقى محلل DXF المحلي الحالي سريعاً، ويضاف موفر `NativeDwgDocumentSource` عندما تتوفر رخصة/ثنائيات ODA أو FastView. لا يتم تحويل DWG إلى صورة فقط؛ بل يجب تمرير كياناته ونقاطه القابلة للالتقاط إلى طبقة القياس نفسها كي تبقى المعايرة والبعد المحفوظان دقيقين.

المراجع: https://www.opendesign.com/products/drawings ، https://en.dwgfastview.com/cloud/cad-sdk/ ، https://aps.autodesk.com/en/docs/model-derivative/v2/overview/
