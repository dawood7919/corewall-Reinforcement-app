# تقرير تنفيذ دعم DWG المحلي

**المشروع:** Core Wall QA/QC  
**الإصدار المتحقق:** `0ada337` — بناء GitHub Actions `32021249108`  
**المؤلف:** Manus AI  
**الحالة:** تم تنفيذ دعم DWG محلياً دون أي تحويل سحابي أو تحويل إلى صورة/PDF.

## الملخص

أُضيف محرك مستقل لقراءة DWG عبر **LibreDWG** داخل التطبيق باستخدام Android NDK وJNI. يُحمّل ملف DWG خارج خيط الواجهة، ويحوّل الكيانات المدعومة إلى نموذج `CadDrawing` القائم بالفعل بإحداثيات CAD/world coordinates. لذلك يستمر العارض وأدوات القياس والمعايرة والتخزين في استخدام الهندسة الأصلية، ولا تعتمد النتائج على بكسلات الشاشة.

> لا يعلن هذا التنفيذ توافقاً كاملاً مع مواصفة DWG. إنه قارئ ميداني محلي للكيانات الموضحة أدناه، وقد تم اختباره على ملفات LibreDWG المرجعية الصحيحة وملف معطوب.

## الملفات التي تغيرت

| الملف أو المسار | التغيير |
|---|---|
| `third_party/libredwg` | submodule مثبت عند `318a874` كمصدر LibreDWG. |
| `app/src/main/cpp/CMakeLists.txt` | إعداد CMake لبناء مكتبة `libcorewall_dwg.so` عبر NDK. |
| `app/src/main/cpp/dwg_jni.cpp` | قراءة DWG، تحويل الهندسة، توسعة INSERT الأساسية، حدود الذاكرة، وتحرير `Dwg_Data`. |
| `app/src/main/java/.../CadDocumentSource.kt` | مصدر CAD موحد يختار DXF أو DWG ويحوّل حمولة JNI إلى النموذج الحالي. |
| `CadModels.kt` | إضافة `Ellipse` و`PointEnt` وحساب حدود القطع الناقص. |
| `CadViewerScreen.kt` | عرض القطع الناقص والنقطة، وتحسين الالتقاط للدوائر والأقواس والقطع الناقص والمضلعات المغلقة. |
| `app/build.gradle.kts` | ربط CMake وNDK `27.2.12479018` مع ARM64 وARMv7. |
| `tools/test_dwg_host.sh` | اختبار المضيف الذي يشغل جسر التحويل نفسه على ملفات DWG مرجعية. |
| `.github/workflows/build-apk.yml` | استرجاع submodule وإعداد NDK/CMake وتشغيل اختبار DWG المرجعي في CI. |
| `THIRD_PARTY_NOTICES.md` و`assets/licenses/LIBREDWG-GPLv3.txt` | إشعار الترخيص ونص GPLv3 الموزع مع التطبيق. |

## الكيانات المدعومة

| الكيان | حالة الدعم | ملاحظات التنفيذ |
|---|---|---|
| `LINE` | مدعوم | نقطة البداية والنهاية بإحداثيات العالم. |
| `LWPOLYLINE` | مدعوم | يدعم الإغلاق، ونقاط المسار، وبروزات `bulge` بعد تقريبها إلى نقاط CAD. |
| `POLYLINE` ثنائي وثلاثي الأبعاد | مدعوم | يستخرج رؤوس `VERTEX_2D` و`VERTEX_3D` ويستخدم إسقاط XY للقياس ثنائي الأبعاد. |
| `ARC` | مدعوم | تُحفظ الزوايا؛ وتحويل INSERT الدوراني يُطبّق على الزوايا. |
| `CIRCLE` | مدعوم | يتحول إلى قطع ناقص عند وجود مقياس INSERT غير منتظم. |
| `ELLIPSE` | مدعوم | يحفظ محوري القطع الناقص ومعلمات البداية والنهاية. |
| `POINT` | مدعوم | يظهر بعلامة تقاطع ويمكن التقاطه. |
| `TEXT` و`MTEXT` | مدعوم | الموضع والارتفاع والدوران والنص؛ يعرض العارض أول 40 محرفاً. |
| `BLOCK`/`INSERT` الأساسي | مدعوم عملياً | توسعة كيانات block الأساسية حتى عمق 8، مع إزاحة ودوران ومقياس. |
| طبقات DWG | مدعومة | أسماء الطبقات وحالات off/frozen تنتقل إلى قائمة الطبقات الحالية. |

## الكيانات غير المدعومة أو المحدودة

الكيانات غير المدرجة في الجدول السابق لا توقف التحميل؛ تسجل كتحذير ويُعرض ما أمكن من الرسم. تشمل القيود الحالية HATCH، SPLINE، DIMENSION، MLEADER، MLINE، XREF الخارجي، 3D solids، meshes، الصور/underlays، الكائنات proxy، والـdynamic blocks المتقدمة. لا تُعرض سمات النص المرتبطة بـINSERT في هذه المرحلة، ولا تُنفذ نماذج 3D أو Layout/Paper Space.

## البناء والإعداد

يتطلب البناء Android NDK `27.2.12479018` وCMake `3.22.1`. تسترجع GitHub Actions الـsubmodule تلقائياً ثم تثبت الأداتين قبل بناء APK. يدعم ملف Gradle `arm64-v8a` و`armeabi-v7a`، وقد تحقق البناء المحلي للمحرك وAPK ARM64، وهو ABI المستهدف لهاتف Samsung Galaxy S25 Ultra.

## الاختبارات المنفذة

| الاختبار | النتيجة |
|---|---|
| بناء JNI/CMake على Android ARM64 | ناجح. |
| تجميع Kotlin بعد دمج مصدر DWG | ناجح. |
| اختبارات وحدة التطبيق الحالية | ناجحة. |
| `Line.dwg` | استخرج `LINE` بنجاح. |
| `Polyline.dwg` | استخرج `POLYLINE` بنجاح. |
| `Ellipse.dwg` | استخرج `ELLIPSE` بنجاح. |
| `Text.dwg` | استخرج `TEXT` بنجاح. |
| `Point.dwg` | استخرج `POINT` بنجاح. |
| ملف DWG معطوب | أعاد حمولة خطأ منظمة دون انهيار. |
| بناء GitHub Actions `32021249108` | ناجح: APK، اختبارات الوحدة، اختبار DWG، فحص Room، ونشر الإصدار. |

## الأداء وإدارة الذاكرة

يتم الاستيراد من `CadViewerScreen` داخل `Dispatchers.IO`؛ لذلك لا يبدأ LibreDWG على خيط الواجهة. يحتفظ JNI بهندسة مؤقتة فقط أثناء إنشاء حمولة التحويل، ثم يستدعي `dwg_free(&dwg)` في جميع مسارات القراءة الناجحة والخاطئة. كما يضع الجسر حداً قدره **200,000 كيان** لتجنب استنزاف الذاكرة، ويحد التحذيرات إلى 64 رسالة. الملفات الكبيرة جداً ستحتاج قياساً ميدانياً على الجهاز؛ لم يُسجّل في هذه المرحلة رقم زمني P95 أو ذاكرة فعلية على S25 Ultra.

## دقة القياس

تظل `CadPoint` وإحداثيات DWG world coordinates هي مرجع القياس. العارض يحول موضع اللمس من الشاشة إلى العالم قبل الالتقاط، و`CadMeasurementStore` يحفظ نقاط القياس والمعايرة بحسب مسار الملف كما كان مع DXF. لذلك تعمل المسافة والطول المستمر والمساحة والمحيط والزاوية ونصف القطر والمعايرة على قيم CAD، لا على بكسلات الرندر. يعتمد مقدار الدقة النهائي على وحدات الملف وصحة هندسته وعلى المعايرة اليدوية عند عدم تعريف `INSUNITS` أو عدم مطابقتها للواقع.

## أثر ترخيص LibreDWG

LibreDWG مرخّص بموجب **GNU GPLv3 أو أحدث**. تضمينه وربطه في APK يقتضي، عند توزيع التطبيق، إتاحة **المصدر الكامل المقابل** للمكونات المشتقة وملفات البناء وإشعارات الحقوق ونص الرخصة للمستخدمين، ومنح حقوق GPL المعتادة في الدراسة والتعديل وإعادة التوزيع. لهذا أضيف نص الرخصة داخل APK وإشعار `THIRD_PARTY_NOTICES.md`. ينبغي عدم توزيع نسخة مغلقة المصدر تتضمن هذا المحرك من دون استيفاء تلك الالتزامات.[1] [2]

## القيود المتبقية

يبقى توافق DWG الجزئي مرتبطاً بقدرة LibreDWG على الإصدار والكيانات الموجودة في الملف. لم يُختبر بعد ملف ميداني خاص بالمستخدم أو كل إصدارات AutoCAD، ولا توجد معاينة ثلاثية الأبعاد أو معالجة كاملة للـXREF والديناميك بلوكس. كما أن بروزات LWPOLYLINE تُقرب إلى نقاط CAD لضمان العرض والالتقاط؛ وهذا مناسب للقياس الميداني لكنه ليس تمثيلاً تحليلياً دائرياً منفصلاً. يوصى باختبار ملفات المستخدم الفعلية، لا سيما الرسومات ذات المراجع الخارجية أو الكائنات المعمارية المتخصصة، قبل الاعتماد عليها في حصر تعاقدي نهائي.

## المراجع

[1]: https://www.gnu.org/software/libredwg/ "LibreDWG — GNU Project"
[2]: https://www.gnu.org/licenses/gpl-3.0.html "GNU General Public License v3.0"
